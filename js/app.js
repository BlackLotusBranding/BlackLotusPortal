document.addEventListener('DOMContentLoaded',()=>{
  const b=document.querySelector('.mobile-menu'); const l=document.querySelector('.links'); if(b&&l)b.addEventListener('click',()=>l.classList.toggle('open'));
  document.querySelectorAll('[data-year]').forEach(e=>e.textContent=new Date().getFullYear());
  const path=location.pathname.split('/').pop()||'index.html'; document.querySelectorAll('.links a').forEach(a=>{if(a.getAttribute('href').endsWith(path)|| (path==='index.html'&&a.getAttribute('href').endsWith('../index.html')))a.classList.add('active')});
  if(matchMedia('(pointer:fine)').matches){document.addEventListener('pointermove',e=>{document.body.style.setProperty('--x',e.clientX+'px');document.body.style.setProperty('--y',e.clientY+'px')},{passive:true});}
  const shell=document.querySelector('.carousel-shell'); if(shell){const track=shell.querySelector('.carousel-track'); const slides=[...shell.querySelectorAll('.slide')]; const dots=[...shell.querySelectorAll('.dot')]; let i=0,t; const show=n=>{i=(n+slides.length)%slides.length;track.style.transform=`translateX(${-i*100}%)`;dots.forEach((d,k)=>d.classList.toggle('active',k===i));}; shell.querySelector('.next')?.addEventListener('click',()=>show(i+1)); shell.querySelector('.prev')?.addEventListener('click',()=>show(i-1)); dots.forEach((d,k)=>d.addEventListener('click',()=>show(k))); const play=()=>t=setInterval(()=>show(i+1),5200); const stop=()=>clearInterval(t); shell.addEventListener('mouseenter',stop); shell.addEventListener('mouseleave',play); show(0); play();}
});
